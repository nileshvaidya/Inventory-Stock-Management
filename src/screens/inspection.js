import { makePlaceholderScreen } from '../placeholderScreen.js';

export const render = makePlaceholderScreen({
  route: '/inspection',
  title: 'Inspection',
  phase: 3,
  description: 'Accept or reject received material, in full or in part, with a rejection reason.',
});
