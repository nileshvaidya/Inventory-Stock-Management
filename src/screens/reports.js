import { makePlaceholderScreen } from '../placeholderScreen.js';

export const render = makePlaceholderScreen({
  route: '/reports',
  title: 'Reports',
  phase: 8,
  description: 'Total available/reserved stock (by work order), short-for-WO items, and below-reorder items.',
});
