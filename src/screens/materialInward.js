import { makePlaceholderScreen } from '../placeholderScreen.js';

export const render = makePlaceholderScreen({
  route: '/material-inward',
  title: 'Material Inward',
  phase: 3,
  description: 'Log receipts against a PO, including partial deliveries across multiple inward entries.',
});
